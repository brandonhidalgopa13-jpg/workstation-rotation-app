<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Permisos para respaldo y sincronización -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" 
        android:maxSdkVersion="28" />
    <uses-permission android:name="android.permission.MANAGE_EXTERNAL_STORAGE" 
        tools:ignore="ScopedStorage" />
    
    <!-- Permisos para sincronización en la nube -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    
    <!-- Permiso para feedback táctil -->
    <uses-permission android:name="android.permission.VIBRATE" />
    
    <!-- Permisos para notificaciones -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <application
        android:name=".RotationApplication"
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher_foreground"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.REWS">
        
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        
        <activity
            android:name=".WorkstationActivity"
            android:exported="false" />
            
        <activity
            android:name=".WorkerActivity"
            android:exported="false" />
            
        <activity
            android:name=".monitoring.RealTimeDashboardActivity"
            android:exported="false"
            android:label="Dashboard en Tiempo Real"
            android:theme="@style/Theme.REWS" />
            
        <activity
            android:name=".SqlRotationActivity"
            android:exported="false"
            android:label="Sistema de Rotación (Legacy)" />
            
        <activity
            android:name=".NewRotationActivity"
            android:exported="false"
            android:label="Sistema de Rotación v4.0"
            android:theme="@style/Theme.REWS.NoActionBar" />
            
        <activity
            android:name=".SettingsActivity"
            android:exported="false" />
            
        <activity
            android:name=".OnboardingActivity"
            android:exported="false"
            android:theme="@style/Theme.REWS"
            android:screenOrientation="portrait" />
            
        <activity
            android:name=".BenchmarkActivity"
            android:exported="false"
            android:label="Benchmark de Algoritmos" />
            
        <activity
            android:name=".RotationHistoryActivity"
            android:exported="false"
            android:label="Historial de Rotaciones" />
            
        <activity
            android:name=".notifications.NotificationSettingsActivity"
            android:exported="false"
            android:label="Configuración de Notificaciones" />
            
        <activity
            android:name=".dashboard.ExecutiveDashboardActivity"
            android:exported="false"
            android:label="Dashboard Ejecutivo" />
            
        <activity
            android:name=".analytics.AdvancedAnalyticsActivity"
            android:exported="false"
            android:label="Analytics Avanzados" />
            
        <!-- FileProvider para compartir archivos de respaldo -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
            
    </application>

</manifest>